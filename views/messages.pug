extends layout

block content
    h1= title
    if user.membership  
        h2 Welcome back #{user.username} you are a member 
    else 
        h3 Welcome back #{user.username} you are #[em not] a member
    h3 messages here:
   
